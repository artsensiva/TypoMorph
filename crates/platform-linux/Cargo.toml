[package]
name = "platform-linux"
version = "0.1.0"
edition = "2021"

[dependencies]
evdev = "0.12"
thiserror = "1"
zbus = { version = "4", default-features = false, features = ["async-io"] }

[dev-dependencies]
tempfile = "3"
